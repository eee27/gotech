/*
const app = new Vue({
    el: '#app',
    data: {
        users: []
    },
    created() {
        fetch('http://localhost:27270/user.ajax')
            .then(response => response.json()
    )
    .then(json => {this.users = json.users
    })
    }


})
*/

